# Web
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1

# Config / models
pydantic>=2.9
pydantic-settings>=2.6
PyYAML>=6.0

# HTTP
httpx>=0.27
beautifulsoup4>=4.12

# The licensing register is published as a weekly XLSX, so reading it is a
# dependency rather than a nicety.
openpyxl>=3.1

# Geometry
shapely>=2.0

# Storage
#
# duckdb is NOT USED. It was intended as the local pipeline working set and
# nothing imports it. Flagged rather than removed because a requirements line
# is not evidence of an implementation, and this file said exactly that about
# BigQuery too until warehouse.py made it true.
duckdb>=1.1
# Used: supply_radar/warehouse.py publishes the lead table.
google-cloud-bigquery>=3.25
google-cloud-secret-manager>=2.20

# Matching / normalisation
rapidfuzz>=3.10
phonenumbers>=8.13
tldextract>=5.1

# LLM
anthropic>=0.40
